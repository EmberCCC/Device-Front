import React from 'react';
import { SearchProps } from '../interface';
declare const Search: <RecordType extends object = any>(props: SearchProps<RecordType>) => React.ReactElement;
export default Search;
